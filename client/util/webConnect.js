'use strict';

var async = require('asyncawait/async');
var await = require('asyncawait/await');
var config = require('../config');

var pingServer = async(function() {
}); 

module.exports = {
    pingServer : pingServer
};